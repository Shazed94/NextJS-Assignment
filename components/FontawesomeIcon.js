import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const FontAwesomeIconComponent = ({ icon, size, color }) => {
    return <FontAwesomeIcon icon={icon} size={size} color={color} />;
};

export default FontAwesomeIconComponent;